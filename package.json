{
  "name": "director",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:desktop": "turbo run dev --filter='!@director/website'",
    "dev:website": "turbo run dev --filter='!@director/desktop'",
    "dev:reset": "bun run clean && bun install",
    "build": "turbo run build",
    "build:desktop": "turbo run build --filter='!@director/website'",
    "build:website": "turbo run build --filter='!@director/desktop'",
    "start": "turbo run start",
    "start:desktop": "turbo run start --filter='@director/desktop'",
    "start:website": "turbo run start --filter='@director/website'",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "check-types": "turbo run check-types",
    "clean": "rm -rf .turbo node_modules && turbo run clean",
    "cli": "turbo run cli",
    "desktop:version": "bun run apps/desktop/scripts/version.ts",
    "desktop:release": "version=$(bun run desktop:version print) && git tag -a \"v${version}\" -m \"Release v${version}\" && git push origin \"v${version}\""
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "tailwindcss": "^4.0.6",
    "turbo": "^2.4.4",
    "typescript": "^5.8.2"
  },
  "packageManager": "bun@1.2.5",
  "workspaces": ["packages/*", "apps/*"],
  "engines": {
    "node": "~23.10.0",
    "bun": "~1.2.5"
  },
  "dependencies": {
    "vitest": "^3.0.9"
  }
}
