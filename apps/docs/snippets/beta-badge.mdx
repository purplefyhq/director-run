export const BetaBadge = () => {
    return (
        <code className="beta-badge-container">
            <p className="beta-badge">
                Director is in BETA and is not yet production ready.
            </p>
        </code>
    );
};