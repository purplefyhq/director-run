# Install via NPM

Director is available from the NPM registry. You can install it via NPM or any other node package manager of your choosing.

```bash
npm install -g @director/run/cli
```

# Verify the Installation

Make sure it’s available in your `$PATH` and installed properly:

```bash
director --help
```